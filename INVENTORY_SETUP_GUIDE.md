# 📦 دليل إعداد نظام المخزون

## الخطوات المطلوبة لتشغيل Inventory Management:

### 1️⃣ افتح صفحة Inventory Management
```
http://localhost:3000/admin/inventory-management
```

### 2️⃣ اضغط زر "Initialize Stock" 🟣
- الزر ده بنفسجي اللون جنب زر Refresh
- هيسأل تأكيد، اضغط OK
- هينشئ حقل `initial_units` لكل منتج بناءً على المخزون الحالي

### 3️⃣ انتظر التنفيذ
- هيظهر رسالة بعدد المنتجات اللي اتحدثت
- اضغط Refresh بعدها علشان تشوف البيانات المحدثة

---

## 📊 كيف يعمل النظام:

### المعادلة الأساسية:
```
المتبقي = العدد الأساسي - اللي خرج + المرتجعات
```

### الأعمدة في الجدول:
| العمود | الوصف |
|--------|-------|
| **Initial Stock** 🟣 | العدد الأساسي (ثابت - لا يتغير إلا يدوياً) |
| **Out** 🔴 | اللي خرج (delivered orders only) |
| **Remaining** 🔵 | المتبقي (يتحدث تلقائياً) |

### التحديثات التلقائية:
- ✅ **عند Delivery**: ينطرح من Remaining
- ✅ **عند Return**: يضاف للـ Remaining
- ✅ **عند Cancel**: يرجع للـ Remaining
- ⚠️ **Initial Stock**: ثابت ولا يتغير

### الضمانات:
- ✅ Remaining ≤ Initial Stock دائماً
- ✅ المرتجعات بترجع تلقائياً للمخزون
- ✅ الطلبات الملغية مش بتأثر على المخزون

---

## 🔧 APIs المستخدمة:

1. **`/api/admin/inventory-overview`**
   - بيجيب البيانات المحسوبة صح
   - بيتعامل مع initial_units من المنتج
   - بيحسب quantityOut من الـ movements

2. **`/api/admin/init-inventory`** (POST)
   - بيهيئ initial_units للمنتجات الموجودة
   - بيتنفذ مرة واحدة بس لكل منتج

---

## ⚠️ ملاحظات مهمة:

1. **لازم تضغط "Initialize Stock" مرة واحدة** في البداية لإعداد المتنجات الموجودة
2. **المنتجات الجديدة** هتاخد initial_units تلقائياً عند الإنشاء
3. **لو عايز تغير Initial Stock** لمنتج معين، لازم تعدله يدوياً في Products page
4. **المرتجعات** بترجع تلقائياً للمخزون من خلال InventoryTracker

---

## 🎯 النتيجة المتوقعة:

بعد الإعداد، الجدول هيعرض:
- ✅ العدد الأساسي الثابت لكل منتج
- ✅ اللي خرج (delivered فقط)
- ✅ المتبقي (يشمل المرتجعات تلقائياً)
- ✅ الحالة (In Stock / Low Stock / Out of Stock)

**المعادلة صحيحة وشغالة! Initial Stock ثابت، واللي بيتغير هو Remaining فقط** ✅